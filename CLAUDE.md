# PlanterAI — U☆PoC 応募提案書

## 概要

電気通信大学の **U☆PoC（ユーザ志向アイディア実証コンテスト）** に応募するための提案書を LaTeX で作成しているプロジェクト。

- メンバー：中村耕介（代表・情報理工学域II類先端ロボティクスプログラム3年）、地引宙翔（情報理工学域II類電子情報学プログラム）
- ファイル：`PlanterAI.tex` → `PlanterAI.pdf`

---

## プロジェクトアイデア：PlanterAI（タイトル：「PlanterAI：植物が語りかけるIoTシステム」）

### コンセプト

生成AIで人格を持つIoTプランター。土壌の乾燥度・温度・日照度などのリアルタイムセンサーデータをもとに、植物が擬人化キャラクターとして自発的に会話・おねだりを発信する育成支援システム。

**着想の原点：** 過去に観葉植物を悪気なく枯らしてしまった経験から、「植物が自分でのどが渇いたと教えてくれたら」という思い。植物をペットのように愛着を持って育てられることが最終目標。

### 解決する課題

| 課題 | 内容 |
|------|------|
| 植物のサイレントデス | 鳴かないため水不足・日照不足のサインに気づきにくい |
| 管理アプリのマンネリ化 | 数値・グラフ通知は飽きて確認を怠るようになる |
| 愛着・感情移入の難しさ | 目に見えるリアクションが少なくモチベーション維持が困難 |

### 仕組み（システム構成）

1. **センサー群によるデータ取得** — 水分量・温度・日照度をマイコン経由で定期収集
2. **LLMへのコンテキスト入力** — センサー値＋植物のキャラ設定（ツンデレ・敬語・のんびり屋など）をプロンプトとして投入
3. **感情とセリフの自動生成** — 「土が乾いて寂しがっている」「日差しが強すぎて怒っている」などをキャラ口調で生成
4. **自発的なプッシュ配信** — LINE or 専用アプリへおねだりとして通知

### 差別化

| 比較項目 | planterAI | 一般スマートプランター | たまごっち等 |
|----------|-----------|----------------------|------------|
| 自発的な「人格のある」会話 | ✓ | × | ○ |
| 物理的な植物（生命）との連動 | ✓ | ✓ | × |
| センサーデータの活用 | ✓ | ✓ | × |
| 飽きないテキストの多様性 | ✓ | × | △ |

### 強み

単なる「可視化」にとどまらず、データと生成AIを掛け合わせて **「感情の可視化」** へ昇華している点。時間帯・会話履歴・植物の機嫌によって毎日異なるメッセージが届くため、本物の生き物と共同生活しているようなエンタメ性と愛着を生む。

### 技術スタック（確定）

| レイヤー | 技術 | 理由 |
|----------|------|------|
| マイコン | ESP32 + 土壌湿度・温度・照度センサー | Wi-Fi内蔵・センサー読み取りからHTTP送信まで一台で完結 |
| サーバー | Next.js API Route (Vercel) | 無料デプロイ・単一リポジトリ管理・TypeScript |
| LLM | Gemini Flash | 高速・無料枠が大きい |
| 通知 | LINE Messaging API | ユーザーの既存アプリで完結、専用アプリ不要 |
| DB | Supabase（無料枠） | 過去ログ保存による人格の連続性に必須 |

### 実装上の重要ルール

- センサー値はESP32側で **5回計測の中央値** を取ってから送信（ノイズ除去）
- **状態判定はルールベース**で先に行い、LLMにはセリフ生成のみ担わせる（ハルシネーション対策）
- **Supabaseに保存するもの**：直近の状態・乾燥継続日数・前回メッセージのトーン → 次回プロンプトで参照
- **LINE通知は1日最大3回 or 状態遷移時**に限定（スパム防止）
- ESP32からVercel(HTTPS)へ送る際は `WiFiClientSecure` + `setInsecure()` でまず動かす

### アプリケーション（確定）

- **双方向LINEチャット**：LINE Webhookでユーザーのメッセージを受信し、キャラ口調で返答。受動的通知から能動的関係へ。
- **キャラクター設定Web UI**：植物の名前・口調（ツンデレ/のんびり屋/敬語）・通知頻度をブラウザで設定。Next.js同一リポジトリ内に実装、設定はSupabaseへ保存。
- **今後の展望**（アプリケーションセクション内に記載）：カメラモジュールで葉の色・萎れ検知、気圧・CO₂センサー追加、複数植物の一括管理アプリ。

### 残課題

- **LLMのAPIコストと省電力化：** 長期運用でのAPIコスト削減・バッテリー駆動時の省電力制御
- **ハルシネーションの制御：** センサーバグ値による不自然な出力を防ぐデータフィルタリング

---

## ビルド方法

```sh
platex PlanterAI.tex
dvipdfmx PlanterAI.dvi
```

## フォーマット要件（厳守）

### レイアウト
- A4縦、シングルカラム
- 余白：上下 25mm、左右 20mm
- ページ数：**2ページ以内**
- カラー原稿可（印刷時は白黒になる場合あり）

### タイトル
- 中央揃え、ゴシック系 14pt
- **25文字以内**
- 行前 12pt・行後 6pt のスペース

### 著者名
- 明朝体 12pt、中央揃え
- 複数人の場合は代表者に下線
- 所属（類・専攻、学年または卒業・修了年）を **1ページ目左下** に記載し、番号で対応させる

### 調布市の「まちの課題」選択テーマ（任意・調布市長賞対象）
- 著者名の次の行、明朝体 12pt、中央揃え
- 選択肢：
  - テーマ①　調布の歴史の発信
  - テーマ②　ゼロカーボンシティの実現に向けた市民等の行動促進
  - テーマ③　商店会における情報伝達の効率化
  - テーマ④　誰でも楽しめるイベント体験の創出
  - テーマ⑤　調布駅前広場でのイベント企画支援

### 概要
- 明朝体 11pt、改行なし、**200字程度**
- 概要前に 1行・概要後に 0.5行 のスペース

### 本文
- 明朝体 10.5pt
- 行頭 1字下げ

## 提案書の必須記載事項

1. **本文冒頭に「想定するユーザ」章を設ける**
   - どんな人がどんな恩恵を受けるか明記
2. **モノ・サービスの詳細を丁寧に記述**
   - 構造、使用機材・ライブラリ、動作の仕組み、使用状況、効果
   - 図・写真を駆使し、非専門家にもわかりやすく

## 審査ポイント（優先度なし・総合評価）

| 観点 | 内容 |
|------|------|
| 独創性 | 着眼点・解決提案のユニークさ |
| 構成力 | 技術的インパクト・実装の工夫 |
| 有用性 | 社会問題への貢献・技術的・社会的インパクト |
| ユーザへの魅力 | ユーザが明確か・対象ユーザの視点での魅力 |

## 注意事項

- リスト規制（輸出管理）に該当する技術を含む場合は経済産業大臣の許可が必要。審査の2次通過合否に影響する。
- 提出時点で記録写真・Web掲載・肖像権を承諾したものとみなす（全メンバー適用）。
- 発表内容は発表日から12ヶ月間の第三者への漏洩禁止を誓約する（全メンバー適用）。
